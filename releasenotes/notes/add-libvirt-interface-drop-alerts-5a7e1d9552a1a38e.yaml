---
features:
  - |
    Added ``LibvirtDomainInterfaceReceiveDrops`` and
    ``LibvirtDomainInterfaceTransmitDrops`` Prometheus alerts that fire at
    severity ``P3`` when a virtual machine network interface drops packets in
    the last five minutes.
