---
fixes:
  - |
    Fixed an issue in the Manila service image where the
    ``fetch-public-ssh-keys`` systemd service could start too early in
    the boot process, before the instance metadata service or network
    was fully available. This caused failures to retrieve and install
    SSH public keys.
