---
fixes:
  - |
    Neutron now runs the ``neutron-fwaas`` database migrations when
    ``neutron_fwaas_enabled`` is turned on. This creates the FWaaS
    tables before the API starts using them and avoids missing-table
    errors for ``firewall_groups_v2``.
