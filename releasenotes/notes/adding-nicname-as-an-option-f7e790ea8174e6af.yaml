---
features:
  - |
    It's now possible to configure DPDK interfaces using the interface names in addition to
    possibly being able to use the ``pci_id`` to ease deploying in heterogeneous environments.
