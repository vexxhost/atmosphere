---
fixes:
  - |
    Add documentation for both ``retention`` and ``keep`` syntax options in PXC
    backup configuration to address compatibility issues across different PXC
    operator versions. Users may need to use different syntax based on their
    PXC operator version.
