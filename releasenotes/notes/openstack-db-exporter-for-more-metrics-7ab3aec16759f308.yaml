---
features:
  - The OpenStack database exporter now collects the majority of OpenStack
    metrics by querying the database directly. Only a few metrics remain
    on the original API-based exporter.
