---
features:
  - |
    Bump Alertmanager from ``v0.27.0`` to ``v0.32.1``. This adds support for
    the native ``incidentio_configs`` receiver, along with newer notification
    integrations, notifier timeout controls, silence improvements, and
    Alertmanager performance and stability fixes from the upstream releases.
