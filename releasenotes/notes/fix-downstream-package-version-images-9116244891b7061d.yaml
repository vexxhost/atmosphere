---
fixes:
  - |
    The default Magnum images include a corrected version of ``pbr``
    that handles downstream package versions. This fixes errors when the
    services read their installed versions.
