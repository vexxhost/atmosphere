---
fixes:
  - |
    Due to the label provisioning_status!="ACTIVE" will be ignore
    when the operational_state is online and this will be
    a problem if some lb stuck at updating process and the alert never firing.