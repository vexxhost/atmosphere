---
fixes:
  - |
    Updated the Octavia image to correctly copy the issuing CA
    certificate's Subject Key Identifier into generated certificate
    Authority Key Identifiers. This fixes certificate validation failures
    when the CA's stored SKI differs from the identifier derived from its
    public key, including CAs generated by cert-manager.