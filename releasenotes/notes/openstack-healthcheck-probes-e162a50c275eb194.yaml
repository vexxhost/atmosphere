---
fixes:
  - |
    The OpenStack Helm API charts for Barbican, Cinder, Designate, Glance,
    Heat, Magnum, Neutron, and Nova now route Kubernetes probes to a
    dedicated ``/healthcheck`` endpoint backed by ``oslo.middleware``. This
    avoids API probe failures when the root path doesn't provide a reliable
    health signal.
