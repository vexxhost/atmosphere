---
fixes:
  - |
    The ``libvirt-tls-sidecar`` image now uses version v1.0.2 instead of
    v1.0.1. This update skips unnecessary certificate reloads when
    certificate files have not changed and reports certificate reload
    errors as failures.
