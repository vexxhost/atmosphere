---
fixes:
  - |
    The ``libvirt-tls-sidecar`` image now uses version v1.0.2 instead of
    v1.0.1. This update suppresses unnecessary certificate reloads when the
    certificate files are unchanged and reports certificate reload errors as
    failures.
