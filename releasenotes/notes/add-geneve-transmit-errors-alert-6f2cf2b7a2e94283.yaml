---
features:
  - |
    Adds ``GeneveTransmitErrors``, a Prometheus alert for sustained transmit
    errors on devices reported by ``node_ethtool_info{driver="geneve"}``.

    This catches host-local ``Geneve`` encapsulation or underlay transmit
    failures that can affect tenant overlay traffic.
