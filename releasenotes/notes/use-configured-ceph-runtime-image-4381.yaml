fixes:
  - |
    The Ceph deployment now passes the configured Ceph image to the upstream
    Ceph collection. This keeps the Ceph bootstrap and client commands on the
    same runtime image and avoids incompatible keyring formats.