---
fixes:
  - |
    The bug was detected when customer env use OVN and they got 10.96.0.20 for nameserves from DHCP Agent.
    We can unset in on atmosphere/roles/neutron/vars/main.yml at line 63 or replace that value with public DNS, e.g. 1.1.1.1.
