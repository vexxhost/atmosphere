---
fixes:
  - |
    The default Magnum image includes a corrected version of ``pbr`` that
    handles downstream package versions. This fixes a startup failure when
    Magnum reads its installed version.
