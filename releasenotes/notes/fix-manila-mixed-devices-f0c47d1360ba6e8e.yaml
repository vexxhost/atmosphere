---
fixes:
  - |
    Resolves the issue where Manila uses the device name for mounting, which may
    change after a Manila server reboot. This fix modifies the mounting mechanism
    to use the device UUID as the identifier, ensuring consistency and reliability.
