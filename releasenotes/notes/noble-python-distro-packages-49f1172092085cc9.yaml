---
fixes:
  - |
    Install OpenStackSDK and PyMySQL from distribution packages on Ubuntu
    newer than 22 and Debian newer than 12, avoiding externally managed
    Python environment errors during deployment. Other systems retain the
    existing pip installation behavior and retries. The
    ``openstacksdk_version`` override applies only to pip installations;
    package installations use the version provided by the distribution.
