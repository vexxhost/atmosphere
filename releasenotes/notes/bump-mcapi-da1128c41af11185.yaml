---
upgrade:
  - |
    Bump the Cluster API driver for Magnum from ``0.33.0`` to ``0.34.0``.
    Fixes Cluster API proxy token expiration issue, and resolves CAPO admission
    webhook prevents updating ``OpenStackCluster`` ``apiServerLoadBalancer``
    spec.
