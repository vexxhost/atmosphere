---
fixes:
  - |
    Use the configured QEMU user and group when setting ownership of
    ``/dev/kvm`` in the libvirt pod. This keeps device ownership aligned with
    ``qemu.conf`` when the host and container assign different numeric IDs to
    ``kvm``.
