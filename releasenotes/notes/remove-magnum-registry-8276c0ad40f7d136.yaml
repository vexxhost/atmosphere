---
upgrade:
  - |
    The Magnum role no longer deploys the unused ``magnum-registry``
    container registry. The registry image, hostname, ingress, and priority
    class settings have been removed. Existing registry deployments,
    services, and ingresses are not deleted automatically and can be
    removed after upgrading.
