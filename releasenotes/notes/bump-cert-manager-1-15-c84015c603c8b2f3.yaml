---
upgrade:
  - |
    The default cert-manager Helm chart now uses ``v1.15.5``.  The deployed
    cert-manager component images are updated to match the chart, replacing the
    old ``cert-manager-ctl`` image with ``cert-manager-startupapicheck``.
