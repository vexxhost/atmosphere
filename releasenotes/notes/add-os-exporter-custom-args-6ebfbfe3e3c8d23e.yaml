---
features:
  - |
    Added support for setting custom arguments for the OpenStack
    exporter via the ``openstack_exporter_args`` variable. Users can now
    customize the behavior of the exporter to better suit their
    monitoring needs.
