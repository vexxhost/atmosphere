---
features:
  - |
    The parallel deploy orchestrator now prioritises admission of
    runnable components by the length of their longest downstream
    dependency chain once the global concurrency cap saturates.
    Components with more remaining work start ahead of shorter ones
    that happen to arrive at the scheduler first, which shortens the
    overall deployment time under bounded concurrency.
