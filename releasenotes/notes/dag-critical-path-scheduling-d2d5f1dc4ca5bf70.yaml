---
features:
  - |
    The parallel deploy orchestrator now prioritises admission of ready
    nodes by the length of their downstream critical path when the
    global concurrency cap is reached. Components with more remaining
    work start ahead of shorter ones that happen to arrive at the
    scheduler first, shortening the overall deployment makespan under
    bounded concurrency.
