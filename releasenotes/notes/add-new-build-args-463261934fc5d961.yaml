---
other:
  - |
    Added support for specifying a custom Git repository for building images.
    Images can now use the ``PROJECT_GIT_REPO`` build arguments, allowing for
    more flexible image building.
