---
upgrade:
  - |
    The minimum required version of ``atmosphere.common`` is now 0.7.1.
    This includes the Secretgen controller manifest rendering fix needed
    for compatibility with newer Ansible templating behavior.
