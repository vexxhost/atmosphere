---
features:
  - |
    Cinder now supports Ceph ``erasure coded`` pools. Operators can create
    EC pools via Rook ``CephBlockPool`` CRDs and configure volume backends
    using the ``rbd_data_pool`` option.
