---
features:
  - |
    Cinder now supports Ceph ``erasure coded`` pools. Operators can configure
    volume backends that provide improved storage efficiency via
    ``conf.ceph.ec_pools`` in the Cinder Helm values.
