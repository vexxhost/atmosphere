---
features:
  - |
    Cinder now supports Ceph erasure-coded (EC) pools. Operators can create
    EC pools via Rook ``CephBlockPool`` and configure volume backends
    using the ``rbd_data_pool`` option.
