---
features:
  - |
    The ``Magnum-cluster-api`` driver updates to version 0.35. When using
    Cilium as the network driver, operators can enable Cilium HubbleUI by
    setting the ``cilium_hubble_ui_enabled`` cluster label.
