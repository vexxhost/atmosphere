---
features:
  - |
    Atmosphere now enables Neutron Firewall as a Service (FWaaS) for OVN
    deployments by default. The deployment adds the ``firewall_v2`` service
    plugin, configures the OVN FWaaS driver, runs the ``neutron-fwaas``
    database migrations, and exposes the Horizon dashboard.
