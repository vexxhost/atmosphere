---
fixes:
  - |
    Fixed the ``NodeNetworkReceiveErrs`` and ``NodeNetworkTransmitErrs`` alert
    descriptions to report the current interface error rate instead of a rounded
    error count. This avoids misleading notifications such as a firing alert
    that claims there were ``0`` errors in the last two minutes.
