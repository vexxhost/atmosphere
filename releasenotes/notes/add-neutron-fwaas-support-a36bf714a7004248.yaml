---
features:
  - |
    Neutron Firewall as a Service (FWaaS) support is now available. Set
    ``neutron_fwaas_enabled`` to ``true`` to turn it on. This configures
    the OVN firewall service driver and adds the ``firewall_v2`` service
    plugin to Neutron. FWaaS is off by default.
