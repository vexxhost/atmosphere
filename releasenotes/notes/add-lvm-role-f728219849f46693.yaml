---
fixes:
  - |
    Fixed an issue where LVM attempted to load logical volumes residing within
    volumes exposed over iSCSI or Fibre Channel to the physical host. This patch
    updates the LVM role to exclude these devices from scanning, preventing
    potential conflicts and ensuring proper device management.
