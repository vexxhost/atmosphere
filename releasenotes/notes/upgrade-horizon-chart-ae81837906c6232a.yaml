---
upgrade:
  - |
    Upgrade the Horizon Helm chart from ``0.3.15`` to ``0.3.16``.
    This version adds support for custom panel configuration,
    allowing operators to enable or disable specific dashboard
    panels through the ``custom_panels`` setting.
