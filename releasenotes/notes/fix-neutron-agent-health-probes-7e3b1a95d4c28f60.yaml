---
fixes:
  - |
    Live health checks for the Dynamic Host Configuration Protocol (DHCP)
    ``agent`` no longer cause unnecessary restarts when ``RabbitMQ``
    connectivity fails, and readiness checks now inspect sockets from every
    matching Neutron process, including the worker processes that own
    established ``RabbitMQ`` connections.
