---
features:
  - |
    The parallel deploy orchestrator now schedules components using a
    per-node readiness model instead of wave barriers. Each component
    starts as soon as all its own dependencies have completed, even
    when other unrelated components in the same topological level are
    still running. This removes "wave gaps" where a short component
    would otherwise wait for an unrelated long one to finish before
    starting.
