---
other:
  - |
    Documented the four known gaps still required to make a Magnum/CAPI
    BM cluster boot end-to-end on the AIO emulator without operator
    intervention: the ``vexxhost.ceph`` loopback guard, missing
    ``server_type=bm`` in the mcapi driver, the k8s-keystone-auth
    chicken-and-egg with kube-apiserver, the ``maxPods=250`` kubelet
    bump, and idempotent kubeadm finalize phases. See
    ``roles/ironic_emulator_nodes/README.md`` for details.
