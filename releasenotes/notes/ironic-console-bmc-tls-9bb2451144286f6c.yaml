---
features:
  - |
    Add optional private CA and TLS policy configuration for Kubernetes-backed
    Ironic graphical console Pods. The role validates the configured Secret
    and key before deployment. Certificate verification can be disabled with
    an explicit operator override.
