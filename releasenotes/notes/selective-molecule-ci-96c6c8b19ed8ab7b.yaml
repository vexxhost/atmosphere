---
features:
  - |
    The planner now maps pull request changes to focused Molecule deployment
    and verification jobs. Selected jobs deploy the dependencies needed for a
    fresh environment, while unrelated scenarios are skipped. Shared, unknown,
    or unplannable changes continue to run the complete test set.
