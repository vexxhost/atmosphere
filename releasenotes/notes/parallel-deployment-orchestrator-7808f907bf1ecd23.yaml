---
features:
  - |
    Adds a parallel deployment orchestrator (``atmosphere deploy``) that uses
    a DAG-based dependency graph to deploy all components across parallel
    waves, reducing full deployment time from ~60 minutes to ~22 minutes.
