---
features:
  - |
    Adds a parallel deployment orchestrator (``atmosphere deploy``) that
    uses a DAG-based dependency graph to deploy components concurrently
    across parallel waves. Components that have no direct dependency on
    each other run in parallel instead of serially, which significantly
    shortens full deployment time compared to the previous sequential
    role-by-role flow.
