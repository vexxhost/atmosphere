This temporary marker exercises local-path-provisioner selective CI planning.
Only the local-path CSI job should deploy the Kubernetes and CSI closure.
The RBD job, both AIO jobs, and Keycloak must skip, and Ceph must not be
prepared or deployed.
