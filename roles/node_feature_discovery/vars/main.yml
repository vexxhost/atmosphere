_node_feature_discovery_helm_values:
  image:
    repository: "{{ lookup('vexxhost.atmosphere.image_ref', 'node_feature_discovery', output='name') }}"
  master:
    nodeSelector:
      openstack-control-plane: enabled
