- name: Upload Helm chart
  run_once: true
  ansible.posix.synchronize:
    src: "{{ upload_helm_chart_src }}"
    dest: "{{ upload_helm_chart_dest }}"
    archive: false
    recursive: true
    checksum: true
    delete: true
