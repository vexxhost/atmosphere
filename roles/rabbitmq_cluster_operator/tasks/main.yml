# Copyright (c) 2023 VEXXHOST, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may
# not use this file except in compliance with the License. You may obtain
# a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.

# NOTE(mnaser): We should get rid of this task eventually as it is suspending
#               the old HelmRelease and removing it to avoid uninstalling the
#               Helm chart.
- name: Uninstall the legacy HelmRelease
  run_once: true
  block:
    - name: Suspend the existing HelmRelease
      failed_when: false
      kubernetes.core.k8s:
        state: patched
        api_version: helm.toolkit.fluxcd.io/v2beta1
        kind: HelmRelease
        name: "{{ rabbitmq_cluster_operator_helm_release_name }}"
        namespace: "{{ rabbitmq_cluster_operator_helm_release_namespace }}"
        definition:
          spec:
            suspend: true

    - name: Remove the existing HelmRelease
      failed_when: false
      kubernetes.core.k8s:
        state: absent
        api_version: helm.toolkit.fluxcd.io/v2beta1
        kind: HelmRelease
        name: "{{ rabbitmq_cluster_operator_helm_release_name }}"
        namespace: "{{ rabbitmq_cluster_operator_helm_release_namespace }}"

- name: Generate diff for Helm values
  when: ansible_diff_mode
  run_once: true
  block:
    - name: Get current Helm release values
      kubernetes.core.helm_info:
        name: "{{ rabbitmq_cluster_operator_helm_release_name }}"
        release_namespace: "{{ rabbitmq_cluster_operator_helm_release_namespace }}"
      register: _rabbitmq_cluster_operator_helm_info

    - name: Display difference between values
      ansible.utils.fact_diff:
        before: "{{ _rabbitmq_cluster_operator_helm_info.status['values']|to_nice_yaml }}"
        after: "{{ _rabbitmq_cluster_operator_helm_values | combine(rabbitmq_cluster_operator_helm_values, recursive=True)|to_nice_yaml }}"
      when: _rabbitmq_cluster_operator_helm_info.status is defined # when another operation is in progress, status is then not defined.

- name: Deploy Helm chart
  run_once: true
  kubernetes.core.helm:
    name: "{{ rabbitmq_cluster_operator_helm_release_name }}"
    chart_ref: "{{ rabbitmq_cluster_operator_helm_chart_ref }}"
    release_namespace: "{{ rabbitmq_cluster_operator_helm_release_namespace }}"
    create_namespace: true
    kubeconfig: /etc/kubernetes/admin.conf
    values: "{{ _rabbitmq_cluster_operator_helm_values | combine(rabbitmq_cluster_operator_helm_values, recursive=True) }}"
