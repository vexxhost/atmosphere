This temporary marker exercises Barbican selective CI planning. Only the
canonical AIO job should deploy Barbican and its functional dependency closure.
The alternate network backend and dedicated scenario jobs must skip.
