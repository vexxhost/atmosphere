This temporary marker exercises RBD CSI selective CI planning.
Only the RBD CSI job should deploy the Kubernetes, Ceph, and CSI closure.
The local-path job, both AIO jobs, and Keycloak must skip. Ceph preparation
must preserve the configured container image and binary environment.
