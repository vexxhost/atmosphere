- name: Install operator
  kubernetes.core.k8s:
    state: present
    template:
      - namespace.yml
      - role.yml
      - service_account.yml
      - role_binding.yml
      - secret.yml
      - deployment.yml
