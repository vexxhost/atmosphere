---
apiVersion: v1
kind: Service
metadata:
  name: atmosphere
  namespace: openstack
  labels:
    application: atmosphere
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    application: atmosphere
