tempest_helm_values:
  conf:
    script:
      tempest run --config-file /etc/tempest/tempest.conf --concurrency 4 --include-list /etc/tempest/test-whitelist
    whitelist:
      - ^octavia_tempest_plugin.tests.scenario.v2.(?!.*traffic_ops)
