- name: Destroy infrastructure
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Delete KinD cluster
      command: kind delete cluster --name {{ item.name }}
      with_items: "{{ molecule_yml.platforms }}"
      loop_control:
        label: "{{ item.name }}"