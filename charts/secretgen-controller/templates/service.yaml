apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretgen-controller.fullname" . }}
  labels:
    {{- include "secretgen-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretgen-controller.selectorLabels" . | nindent 4 }}
