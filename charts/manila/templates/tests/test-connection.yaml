apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manila.fullname" . }}-test-connection"
  labels:
    {{- include "manila.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manila.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
