apiVersion: v1
kind: Service
metadata:
  name: {{ include "manila.fullname" . }}
  labels:
    {{- include "manila.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manila.selectorLabels" . | nindent 4 }}
