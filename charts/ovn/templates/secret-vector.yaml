{{/*
Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

   http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/}}

{{- if .Values.pod.sidecars.vector }}

---
apiVersion: v1
kind: Secret
metadata:
  name: ovn-vector-config
type: Opaque
stringData:
  vector.toml: |
    [sources.file_logs]
    type = "file"
    include = [ "/logs/ovn-controller.log" ]

    [sinks.ovn_log_parser_in]
    type = "http"
    inputs = ["file_logs"]
    uri = "{{ tuple "ovn_logging_parser" "default" "api" . | include "helm-toolkit.endpoints.keystone_endpoint_uri_lookup" }}"
    encoding.codec = "json"
    method = "post"

    [sources.ovn_log_parser_out]
    type = "http_server"
    address = "0.0.0.0:5001"
    encoding = "json"

    [transforms.parse_log_message]
    type = "remap"
    inputs = ["ovn_log_parser_out"]
    source = '''
      del(.source_type)
      del(.file)
      del(.host)
      parsed = parse_regex!(.message, r'name="(?P<name>[^"]+)", verdict=(?P<verdict>\w+), severity=(?P<severity>\w+), direction=(?P<direction>[\w-]+): (?P<protocol>\w+),vlan_tci=(?P<vlan_tci>0x[0-9a-fA-F]+),dl_src=(?P<dl_src>[0-9a-f:]+),dl_dst=(?P<dl_dst>[0-9a-f:]+),nw_src=(?P<nw_src>[\d.]+),nw_dst=(?P<nw_dst>[\d.]+),nw_tos=(?P<nw_tos>\d+),nw_ecn=(?P<nw_ecn>\d+),nw_ttl=(?P<nw_ttl>\d+),nw_frag=(?P<nw_frag>\w+),tp_src=(?P<tp_src>\d+),tp_dst=(?P<tp_dst>\d+),tcp_flags=(?P<tcp_flags>\w+)')
      if parsed != null {
        .name = parsed.name
        .verdict = parsed.verdict
        .severity = parsed.severity
        .direction = parsed.direction
        .protocol = parsed.protocol
        .vlan_tci = parsed.vlan_tci
        .dl_src = parsed.dl_src
        .dl_dst = parsed.dl_dst
        .nw_src = parsed.nw_src
        .nw_dst = parsed.nw_dst
        .nw_tos = parsed.nw_tos
        .nw_ecn = parsed.nw_ecn
        .nw_ttl = parsed.nw_ttl
        .nw_frag = parsed.nw_frag
        .tp_src = parsed.tp_src
        .tp_dst = parsed.tp_dst
        .tcp_flags = parsed.tcp_flags
        del(.message)
      } else {
        log("Failed to parse log message", rate_limit_secs: 30)
      }
    '''

    [sinks.loki_sink]
    type = "loki"
    labels.event_source = "network_logs"
    inputs = ["parse_log_message"]
    endpoint = "http://loki.monitoring:3100"
    encoding.codec = "json"
    tenant_id = "{{`{{ project_id }}`}}"
{{- end }}