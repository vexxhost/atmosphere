- name: Run chart-vendor
  ansible.builtin.include_role:
    name: go
  vars:
    go_command:
      run main.go
       --charts-root {{ chart_vendor_charts_root }}
       --config-file {{ chart_vendor_config_file }}
