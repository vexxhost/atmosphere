[defaults]
# Increase timeout for Galaxy API requests to handle slow/overloaded servers
timeout = 60

[galaxy]
# Configure Galaxy server settings
server_list = release_galaxy

[galaxy_server.release_galaxy]
url = https://galaxy.ansible.com/api/
timeout = 180
